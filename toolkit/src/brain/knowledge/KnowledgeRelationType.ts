export enum KnowledgeRelationType {
    DependsOn = "depends-on",
    References = "references",
    Contains = "contains",
    Imports = "imports",
    Calls = "calls",
    Implements = "implements",
    Extends = "extends",
    Uses = "uses"
}
