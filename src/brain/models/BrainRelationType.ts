export enum BrainRelationType {
    DependsOn = "depends_on",

    Contains = "contains",

    Uses = "uses",

    Implements = "implements",

    Generates = "generates",

    Detects = "detects",

    Recommends = "recommends",

    RelatedTo = "related_to"
}

